# 보안 사고 기록 — 2026-04-13

## 요약
Discord 알림 웹훅 URL이 git 추적 파일(`CLAUDE.md`)에 평문으로 노출되어 있었음. Phase 0 보안 감사에서 발견.

## 노출 경로
- 파일: `CLAUDE.md` (L241, "개발 방식" 섹션)
- 형태: `웹훅 URL: https://discord.com/api/webhooks/1490.../vhy...` 평문 전체
- 기간: `0490e7f` 이전부터 (최소 2026-04-12 이전)
- git 히스토리: **제거 후에도 히스토리에 남음** → 공개 레포였다면 이미 유효 노출

## 조치
1. `CLAUDE.md` L241 평문 URL 제거 → `.env의 DISCORD_NOTIFY_WEBHOOK 사용` 로 대체
2. `.env`에 `DISCORD_NOTIFY_WEBHOOK=...` 추가 (이 파일은 `.gitignore`에 포함 확인됨)
3. `git log` 상 히스토리 정리는 보류 (private repo 가정, 힘들면 `git filter-repo` 옵션 있음)

## 권장 추가 조치 (선택)
- [ ] Discord 웹훅 **재생성** (기존 URL 무효화) — 위 URL이 공격자 손에 들어가면 무단 메시지 스팸 가능
- [ ] 과거 커밋에서 URL 등장 지점 확인: `git log -p -S "discord.com/api/webhooks"`

## 재발 방지
- `.gitignore`에 `.env` 포함 확인됨 (pre-existing)
- `security-guard` 에이전트가 상시 시크릿 감사 담당
- 새 시크릿 필요 시 → 처음부터 `.env` 로, 문서/코드 평문 금지
