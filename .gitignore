__pycache__/
*.py[cod]
*.egg-info/
dist/
.venv/
venv/
.env
data/
logs/*.log
bot.log
log.txt
test_log.txt
.ruff_cache/
.pytest_cache/

# DB 파일 (data/는 위에서 막혔지만 루트나 다른 경로 누수 방지)
*.db
*.db-shm
*.db-wal

# 한글 메모/스크래치 (PRD/HANDOFF 같은 영구 문서는 명시 화이트리스트로 별도 관리)
크림봇 프로젝트 현황*.txt
참고자료*.txt
*.scratch.txt

# scan-debugger 등 임시 probe 스크립트
scripts/probe_*.py
scripts/test_available_sizes.py
scripts/repro_*.py

# 봇 stdout 회전본 (수십 MB) — 영구 보존 가치 X, 디버깅 끝나면 삭제
logs/bot.out
logs/bot.out.prev_*

# Stage 판정 스케줄러 산출물 (run-time artefact)
logs/stage*.out
logs/stage*.lock
logs/stage*_webhook_response.txt
logs/stage_chain*

# 매 세션마다 sessionId/pid 갱신되는 Claude 스케줄러 lock
.claude/scheduled_tasks.lock

# 훅 런타임 (세션별 작업계약·증거게이트 카운터·Fable 게이트 상태)
.claude/runtime/
.claude/.orchestration-state
.claude/.orchestration/
# active.md 는 토글이 만드는 복사본 — 원본은 fable-mode.md / off.md
.claude/orchestration/active.md

# Codex 협업 산출물 (검증 결과·raw 로그·큐) — 한도 소모 기록이라 커밋하지 않는다
reports/codex/

# Superpowers brainstorming workspace
.superpowers/

# Git worktrees (isolated branch workspaces)
.worktrees/
